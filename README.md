# NOKIA-CLI
simulador terminal nokia CLI
# 📟 NOKIA CLI SIMULATOR (Treinamento Técnico)

Este projeto é um **Simulador de Interface de Linha de Comando (CLI) da NOKIA**, desenvolvido para fins de treinamento e capacitação técnica em rotinas de configuração de equipamentos.

O objetivo principal é oferecer um ambiente seguro e interativo para a prática da sintaxe de comandos sem risco de afetar sistemas reais.

---

## 🛠️ Tecnologias Utilizadas

Este simulador foi construído utilizando as seguintes tecnologias:

* **Backend:** PHP
* **Frontend:** HTML, CSS, JavaScript
* **Comunicação:** AJAX (para comunicação assíncrona com o servidor PHP)

---

## ⚙️ Como Instalar e Executar Localmente

Como este é um projeto baseado em PHP, ele **requer um servidor web local** para processar os arquivos.

### 1. Pré-requisitos (Servidor Local)

Você precisará de um ambiente de servidor local configurado (ex: XAMPP, WAMP ou MAMP).

* **Instale:** Baixe e instale um dos pacotes de servidor (XAMPP é altamente recomendado).
* **Inicie:** Certifique-se de que o serviço **Apache** (servidor web) esteja **iniciado e em execução**.

### 2. Configuração do Projeto

1.  **Localize a Pasta Raiz:** Encontre a pasta principal do seu servidor web. Ela é chamada:
    * `htdocs` (no XAMPP)
    * `www` (no WAMP/MAMP)

2.  **Mova os Arquivos:** Coloque a pasta completa do projeto (`nokia-cli-simulator`) dentro da pasta **`htdocs`** (ou `www`).

### 3. Acesso e Credenciais

1.  **Acesse via Navegador:** Abra seu navegador e acesse o endereço, substituindo `[NOME-DA-PASTA]` pelo nome da pasta do seu projeto (ex: `nokia-cli-simulator`):
    ```
    http://localhost/[NOME-DA-PASTA]
    ```

2.  **Credenciais de Acesso (Simulador):**
    * **Login:** `admin`
    * **Senha:** `admin123`

---

## 💻 Comandos e Funcionalidades

*(Nesta seção, você listaria os comandos de simulação que seu aplicativo suporta, como no exemplo anterior. Ex:* `show version`, `configure port`, etc.)

## 👤 Autor

**Desenvolvido por:** Jorge Moreno
* https://www.linkedin.com/in/jorge-moreno-294895391/
* https://github.com/jorgemorenopd/
